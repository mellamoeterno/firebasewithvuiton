// expect(element).toHaveTextContent(/react/i) is a testing function that 
// is commonly used in testing frameworks such as Jest or 
// Jasmine in the context of testing React applications.

// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';
//this is just a import code, ready to be used, not being completely aplied as a component, not going to give any bugs! prob